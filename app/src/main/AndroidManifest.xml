<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whazzup">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.whazzup.WhazzupApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name="com.example.whazzup.MainActivity" />
        <activity android:name="com.example.whazzup.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.whazzup.rest.RandomResultActivity" />
        <activity android:name="com.example.whazzup.rest.RequestActivity" />
        <activity android:name="com.example.whazzup.QuestioningActivity" />
        <activity android:name="com.example.whazzup.QuestionOpenActivity" />
    </application>

</manifest>